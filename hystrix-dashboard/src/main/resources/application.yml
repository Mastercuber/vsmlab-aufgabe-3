spring:
  application:
    name: hystrix-turbine
server:
  port: 8097
eureka:
  client:
    healthcheck:
      enabled: true
    # Da dies das Register selbst ist wird sich weder registriert, noch die Service Registry abgerufen
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8088/eureka}


debug: false
hystrix:
  dashboard:
    proxyStreamAllowList: '*'
turbine:
  app-config: user-service, role-service, product-service, category-service, product-composite-service
  cluster-name-expression: new String("default")
